#include <iostream>
#include "../include/UndirectedGraph.h"
#define VERBOSE = True

int main(int argc, const char* argv[]) {

	for()

	Graph graph = generateData(5,8);

	if (VERBOSE){
		printGraph(graph);
		std::cout << "\n";
	}
	

	return 0;
}